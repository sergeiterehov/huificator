/**
 * Алфавит хуефикации.
 *
 * Все наборы — в нижнем регистре: индексы в алгоритме считаются по копии слова,
 * полученной через `toLowerCase()`, поэтому поиск не зависит от регистра.
 */

/** Гласные, которые ищутся в слове. */
export const VOWELS = ["а", "у", "о", "ы", "и", "э", "я", "ю", "ё", "е"] as const;

/** Согласные, до первой из которых сокращается окончание. */
export const CONSONANTS = [
    "б", "в", "г", "д", "ж", "з", "й", "к", "л", "м", "н",
    "п", "р", "с", "т", "ф", "х", "ц", "ч", "ш", "щ",
] as const;

/** Замена последней гласной основы перед приставкой. */
export const VOWEL_MAP: Readonly<Record<string, string | undefined>> = {
    "а": "я",
    "у": "ю",
    "о": "е",
    "ы": "и",
    "и": "и",
    "э": "е",
    "я": "я",
    "ю": "ю",
    "ё": "е",
    "е": "е",
};

/** Приставка по умолчанию. */
export const DEFAULT_PREFIX = "ху";
